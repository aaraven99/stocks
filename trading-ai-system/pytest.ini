[pytest]
testpaths = tests
norecursedirs = pytest-cache-files-* .cache artifacts
# The desktop Windows sandbox can leave pytest's cache directory read-only;
# disabling the optional cache keeps local and CI test output deterministic.
addopts = -ra -p no:cacheprovider
